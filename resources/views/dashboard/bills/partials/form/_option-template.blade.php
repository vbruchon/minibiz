<div id="bill-option-model" class="hidden">
  <fieldset class="border border-border rounded-lg p-4 
                bg-muted/10
                backdrop-blur-sm transition">
    <legend class="text-sm font-semibold text-muted-foreground mb-2" data-option-name>Nom option</legend>
    <div data-option-values>
      <label class="flex items-center gap-2 text-sm mb-1" data-option-value>
        <input
          type="{type}"
          name="{name}"
          value="{value}"
          data-price="{price}"
          class="accent-primary"
          {checked}>
        <span>{label}</span>
      </label>
    </div>
  </fieldset>
</div>